export default class Todo {
  constructor(elem) {
    this.createName = elem.createName;
    this.add = elem.add;
    this.check = elem.check;
    this.save = elem.save;
    this.clear = elem.clear;
  }
}
